{
  "name": "fileboss",
  "version": "2.0.0",
  "description": "FileBoss - CaseBuilder Operator Edition",
  "main": "main.py",
  "scripts": {
    "build": "pip install -r requirements.txt && pip install -r requirements-prod.txt",
    "start": "uvicorn main:app --host 0.0.0.0 --port ${PORT:-8000}",
    "dev": "uvicorn main:app --host 0.0.0.0 --port 8000 --reload"
  },
  "engines": {
    "node": ">=18.0.0",
    "python": ">=3.9.0"
  },
  "keywords": [
    "fileboss",
    "casebuilder",
    "fastapi",
    "python"
  ],
  "author": "GlacierEQ",
  "license": "MIT"
}
